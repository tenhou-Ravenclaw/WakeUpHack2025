function agreeWorkBuildByNPO(params) {
    // ホームレスが働くことに問題ないことを確認を受け取った際の処理
    // contactNPOForWorkBuildを呼び出す
}

function WorkBuildByOwner(params) {// 引数として、空き家のidとホームレスのidを受け取る
    // ホームレスが働くことに問題ないことを確認を受け取った際の処理
    // dbに書き加える
}